# Chute — Claude Code Contract

Chute: a macOS utility for people who drive coding agents all day — turns a Finder selection
into agent-ready context, and turns agent output back into files. Offline, no telemetry.

## Session Start

Before anything, read `handoff/NEXT.md` — GENERATED by the handoff-journal plugin, never
hand-edited. The `claude-guardrails` plugin enforces project rules via seven hooks (bash-guard, context-budget, file-claim-guard, post-edit-format, pre-compact-snapshot, pre-write-guard, stop-gate) configured in `.claude/guardrails.json`. Write with `handoff-note <kind> "<title>"`. Close-out:

```
handoff-note done "<what> — \`<verify command>\` → <n>/<n>"
handoff-check
git push
```

## Verification

No single `gate` script — canonical sequence lives in the handoff head's STATE block, re-derive:

```
swift build -c release && swift run -c release chutetests
cd /Users/sxope/Documents/2026/Development/37.chute && CHUTE_HEADLESS=1 ./Scripts/smoke.sh
```
